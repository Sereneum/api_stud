import React from 'react';

const PreCourseList = ({courses, activeCourse, isActiveConfig, funcActivateCourse, clickOnConfig}) => {
    return (
        <div>

        </div>
    );
};

export default PreCourseList;